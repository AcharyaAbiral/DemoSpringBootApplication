spring:
  application:
    name: deliverySystem

  datasource:
    url: jdbc:mysql://localhost:3306/new_delivery_system_db
    username: ${USERNAME}
    password: ${PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
